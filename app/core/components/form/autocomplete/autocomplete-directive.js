angular.module('GO.core').directive('goAutocomplete', function($filter, $locale) {
    return {
        restrict: 'A',
        link: function(scope, element, attr, ngModel) {
			
			
			       
        } // link
    }; // return
}); // module